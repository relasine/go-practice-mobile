import React, { Component } from "react";

import {
  Stylesheet,
  View,
  Scrollview,
  Text,
  TextInput,
  Button
} from "react-native";

export default class PracticeCard extends Component {
  constructor() {
    super();

    this.state = {};
  }

  render() {
    return (
      <ScrollView>
        <View />
      </ScrollView>
    );
  }
}
